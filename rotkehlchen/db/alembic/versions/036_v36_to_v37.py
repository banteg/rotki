"""Upgrade from v36 to v37

Revision ID: 036_v36_to_v37
Revises: 035_v35_to_v36
Create Date: 2025-01-06

Original upgrade function from rotkehlchen v36_v37.py
"""
from alembic import op
import sqlalchemy as sa
from sqlalchemy import CHAR, INTEGER, TEXT, VARCHAR, BLOB

# revision identifiers, used by Alembic.
revision = '036_v36_to_v37'
down_revision = '035_v35_to_v36'
branch_labels = None
depends_on = None


def upgrade() -> None:
    """Upgrade from v36 to v37"""
    # Operations ported from the original upgrade script
    op.execute("'SELECT tx_hash from evm_transactions'")
    op.execute("'SELECT parent_identifier FROM history_events_mappings WHERE name=? AND value=?',\n        (HISTORY_MAPPING_KEY_STATE, HISTORY_MAPPING_STATE_CUSTOMIZED")
    op.execute('"INSERT OR IGNORE INTO location(location, seq')
    op.execute('"INSERT OR IGNORE INTO location(location, seq')
    op.execute("'SELECT chain_id, tx_hash FROM evm_transactions WHERE '\n            'tx_hash IN (SELECT event_identifier FROM history_events")
    op.execute('"DELETE FROM history_events_mappings WHERE name=\'chain_id\'"')
    op.create_table('evm_events_info', ...)
    op.create_table('eth_staking_events_info', ...)
    op.create_table('history_events_copy', ...)
    op.execute("'SELECT * from history_events'")
    op.execute("'INSERT INTO evm_events_info VALUES(?, ?, ?, ?, ?, ?")
    op.drop_table('history_events'')
    op.execute("'ALTER TABLE history_events_copy RENAME TO history_events'")
    op.drop_table('eth2_deposits'')
    op.execute("'DELETE FROM used_query_ranges WHERE name LIKE ?',\n            ('eth2_deposits%',")
    op.execute('"SELECT identifier, amount, usd_value FROM history_events WHERE location=\'B\' AND "\n            "asset=? AND type=\'staking\' AND subtype=\'reward\' AND CAST(amount AS REAL')
    op.execute('"SELECT identifier, amount, usd_value FROM history_events WHERE location=\'B\' AND "\n            "type=\'trade\' AND subtype=\'none\'",')
    op.execute('"SELECT identifier, amount, usd_value FROM history_events WHERE location=\'B\' AND "\n            "type=\'withdrawal\'",')
    op.execute('"SELECT identifier, event_identifier, type, amount, usd_value FROM history_events "\n            "WHERE location=\'B\' AND type IN (\'spend\', \'receive\'')
    op.execute("'DELETE FROM user_credentials WHERE location IN (?, ?")
    op.execute("'DELETE FROM user_credentials_mappings WHERE credential_location IN (?, ?")
    op.execute("'DELETE FROM used_query_ranges WHERE name LIKE ? ESCAPE ?;',\n            (f'{Location.FTX!s}\\\\_%', '\\\\'")
    op.execute("'DELETE FROM used_query_ranges WHERE name LIKE ? ESCAPE ?;',\n            (f'{Location.FTXUS!s}\\\\_%', '\\\\'")
    op.execute('"SELECT value FROM settings WHERE name=\'non_syncing_exchanges\'",')
    op.execute('"UPDATE settings SET value=? WHERE name=\'non_syncing_exchanges\'",\n                (json.dumps(new_values')
    op.execute('"UPDATE settings SET name=\'ssf_graph_multiplier\' WHERE name=\'ssf_0graph_multiplier\'"')
    op.execute('querystr, bindings')
    op.execute("'DELETE from evm_tx_mappings WHERE tx_hash=? AND value=?',\n        [(tx_hash, 0")
    op.execute("'UPDATE history_events SET location=? WHERE event_identifier=?', update_tuples,")
    op.execute("'INSERT INTO history_events_copy VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?")
    op.execute("'UPDATE history_events SET type=?, subtype=?, amount=?, usd_value=?, notes=? WHERE identifier=?',  # noqa: E501\n                update_tuples,")
    op.execute("'UPDATE history_events SET type=?, subtype=?, amount=?, usd_value=? WHERE identifier=?',  # noqa: E501\n                update_tuples,")
    op.execute("'UPDATE history_events SET amount=?, usd_value=? WHERE identifier=?',\n                update_tuples,")
    op.execute("'UPDATE history_events SET type=?, subtype=?, amount=?, usd_value=? WHERE identifier=?',  # noqa: E501\n                update_tuples,")
    op.create_table('evm_events_info', ...)
    op.create_table('eth_staking_events_info', ...)
    op.create_table('history_events_copy', ...)
    op.execute('ALTER TABLE history_events_copy RENAME TO history_events\')\n        write_cursor.switch_foreign_keys(\'ON\')\n\n    @progress_step(description=\'Updating ens mappings schema.\')\n    def _update_ens_mappings_schema(write_cursor: \'DBCursor\') -> None:\n        update_table_schema(\n            write_cursor=write_cursor,\n            table_name=\'ens_mappings\',\n            schema="""address TEXT NOT NULL PRIMARY KEY,\n            ens_name TEXT UNIQUE,\n            last_update INTEGER NOT NULL,\n            last_avatar_update INTEGER NOT NULL DEFAULT 0""",\n            insert_columns=\'address, ens_name, last_update\',\n            insert_order=\'(address, ens_name, last_update)\',\n        )\n\n    @progress_step(description=\'Deleting old tables.\')\n    def _delete_old_tables(write_cursor: \'DBCursor\') -> None:\n        """Deletes old tables that are now unused along with related data\n        """\n        write_cursor.execute(\'DROP TABLE IF EXISTS eth2_deposits\')\n        write_cursor.execute(\n            \'DELETE FROM used_query_ranges WHERE name LIKE ?\',\n            (\'eth2_deposits%\',),\n        )\n\n    @progress_step(description=\'Fixing Kraken events.\')\n    def _fix_kraken_events(write_cursor: \'DBCursor\') -> None:\n        """\n        Fix kraken events with negative amounts related to:\n        - staking ETH after the merge\n        - trades not having subtypes and having negative amounts\n        - withdrawals use positive amounts\n        - instant swaps\n        Needs to be executed after _update_history_events_schema\n        """\n        write_cursor.execute(\n            "SELECT identifier, amount, usd_value FROM history_events WHERE location=\'B\' AND "\n            "asset=? AND type=\'staking\' AND subtype=\'reward\' AND CAST(amount AS REAL) < 0",\n            (A_ETH2.identifier,),\n        )\n        update_tuples: list[Any] = [(\n            HistoryEventType.INFORMATIONAL.serialize(),\n            HistoryEventSubType.NONE.serialize(),\n            str(-FVal(event_row[1])),\n            str(-FVal(event_row[2])),\n            \'Automatic virtual conversion of staked ETH rewards to ETH\',\n            event_row[0],\n        ) for event_row in write_cursor]\n        if len(update_tuples) != 0:\n            write_cursor.executemany(\n                \'UPDATE history_events SET type=?, subtype=?, amount=?, usd_value=?, notes=? WHERE identifier=?\',  # noqa: E501\n                update_tuples,\n            )\n\n        log.debug(\'Fixing kraken trades\')\n        write_cursor.execute(\n            "SELECT identifier, amount, usd_value FROM history_events WHERE location=\'B\' AND "\n            "type=\'trade\' AND subtype=\'none\'",\n        )\n\n        trade_db_type = HistoryEventType.TRADE.serialize()\n        update_tuples = []\n        for event_row in write_cursor:\n            asset_amount = FVal(event_row[1])\n            usd_value = FVal(event_row[2])\n            if asset_amount < ZERO:\n                event_subtype = HistoryEventSubType.SPEND\n                asset_amount = -asset_amount\n                usd_value = -usd_value\n            else:\n                event_subtype = HistoryEventSubType.RECEIVE\n\n            update_tuples.append(\n                (\n                    trade_db_type,\n                    event_subtype.serialize(),\n                    str(asset_amount),\n                    str(usd_value),\n                    event_row[0],\n                ),\n            )\n\n        if len(update_tuples) != 0:\n            write_cursor.executemany(\n                \'UPDATE history_events SET type=?, subtype=?, amount=?, usd_value=? WHERE identifier=?\',  # noqa: E501\n                update_tuples,\n            )\n\n        log.debug(\'Fixing kraken withdrawals\')  # deposits are all positive amount already\n        write_cursor.execute(\n            "SELECT identifier, amount, usd_value FROM history_events WHERE location=\'B\' AND "\n            "type=\'withdrawal\'",\n        )\n        update_tuples = [(\n            str(-FVal(event_row[1])),\n            str(-FVal(event_row[2])),\n            event_row[0],\n        ) for event_row in write_cursor]\n        if len(update_tuples) != 0:\n            write_cursor.executemany(\n                \'UPDATE history_events SET amount=?, usd_value=? WHERE identifier=?\',\n                update_tuples,\n            )\n\n        log.debug(\'Fixing kraken instant swaps\')\n        update_tuples = []\n        grouped_events: dict[str, list[Any]] = defaultdict(list)\n        write_cursor.execute(\n            "SELECT identifier, event_identifier, type, amount, usd_value FROM history_events "\n            "WHERE location=\'B\' AND type IN (\'spend\', \'receive\') AND subtype=\'none\'",\n        )\n        for row in write_cursor:\n            grouped_events[row[1]].append(row)\n\n        for events in grouped_events.values():\n            if len(events) != 2:\n                # we are looking for instant swaps that are always pairs of spend/receive\n                # with the same event identifier\n                continue\n\n            for event in events:\n                if event[2] == \'spend\':\n                    event_subtype = HistoryEventSubType.SPEND\n                    asset_amount = FVal(event[3])\n                    asset_amount_str = str(-asset_amount) if asset_amount != ZERO else str(asset_amount)  # noqa: E501\n                    usd_value_str = str(-FVal(event[4]))\n                else:\n                    event_subtype = HistoryEventSubType.RECEIVE\n                    asset_amount_str = event[3]\n                    usd_value_str = event[4]\n\n                update_tuples.append(\n                    (\n                        trade_db_type,\n                        event_subtype.serialize(),\n                        asset_amount_str,\n                        usd_value_str,\n                        event[0],\n                    ),\n                )\n        if len(update_tuples) != 0:\n            write_cursor.executemany(\n                \'UPDATE history_events SET type=?, subtype=?, amount=?, usd_value=? WHERE identifier=?\',  # noqa: E501\n                update_tuples,\n            )\n\n    @progress_step(description=\'Trimming daily stats.\')\n    def _trim_daily_stats(write_cursor: \'DBCursor\') -> None:\n        """Decreases the amount of data in the daily stats table"""\n        update_table_schema(\n            write_cursor=write_cursor,\n            table_name=\'eth2_daily_staking_details\',\n            schema="""validator_index INTEGER NOT NULL,\n            timestamp INTEGER NOT NULL,\n            pnl TEXT NOT NULL,\n            FOREIGN KEY(validator_index) REFERENCES eth2_validators(validator_index) ON UPDATE CASCADE ON DELETE CASCADE,\n            PRIMARY KEY (validator_index, timestamp)""",  # noqa: E501\n            insert_columns=\'validator_index, timestamp, pnl\',\n            insert_where=\'start_amount != 0 OR end_amount !=0 OR amount_deposited != 0\',\n        )\n\n    @progress_step(description=\'Removing FTX data.\')\n    def _remove_ftx_data(write_cursor: \'DBCursor\') -> None:\n        """Removes FTX-related settings from the DB"""\n        write_cursor.execute(\n            \'DELETE FROM user_credentials WHERE location IN (?, ?)\',\n            (Location.FTX.serialize_for_db(), Location.FTXUS.serialize_for_db()),\n        )\n        write_cursor.execute(\n            \'DELETE FROM user_credentials_mappings WHERE credential_location IN (?, ?)\',\n            (Location.FTX.serialize_for_db(), Location.FTXUS.serialize_for_db()),\n        )\n        write_cursor.execute(\n            \'DELETE FROM used_query_ranges WHERE name LIKE ? ESCAPE ?;')
    op.execute('DROP TABLE history_events\')\n        write_cursor.execute(\'ALTER TABLE history_events_copy RENAME TO history_events\')\n        write_cursor.switch_foreign_keys(\'ON\')\n\n    @progress_step(description=\'Updating ens mappings schema.\')\n    def _update_ens_mappings_schema(write_cursor: \'DBCursor\') -> None:\n        update_table_schema(\n            write_cursor=write_cursor,\n            table_name=\'ens_mappings\',\n            schema="""address TEXT NOT NULL PRIMARY KEY,\n            ens_name TEXT UNIQUE,\n            last_update INTEGER NOT NULL,\n            last_avatar_update INTEGER NOT NULL DEFAULT 0""",\n            insert_columns=\'address, ens_name, last_update\',\n            insert_order=\'(address, ens_name, last_update)\',\n        )\n\n    @progress_step(description=\'Deleting old tables.\')\n    def _delete_old_tables(write_cursor: \'DBCursor\') -> None:\n        """Deletes old tables that are now unused along with related data\n        """\n        write_cursor.execute(\'DROP TABLE IF EXISTS eth2_deposits\')\n        write_cursor.execute(\n            \'DELETE FROM used_query_ranges WHERE name LIKE ?\',\n            (\'eth2_deposits%\',),\n        )\n\n    @progress_step(description=\'Fixing Kraken events.\')\n    def _fix_kraken_events(write_cursor: \'DBCursor\') -> None:\n        """\n        Fix kraken events with negative amounts related to:\n        - staking ETH after the merge\n        - trades not having subtypes and having negative amounts\n        - withdrawals use positive amounts\n        - instant swaps\n        Needs to be executed after _update_history_events_schema\n        """\n        write_cursor.execute(\n            "SELECT identifier, amount, usd_value FROM history_events WHERE location=\'B\' AND "\n            "asset=? AND type=\'staking\' AND subtype=\'reward\' AND CAST(amount AS REAL) < 0",\n            (A_ETH2.identifier,),\n        )\n        update_tuples: list[Any] = [(\n            HistoryEventType.INFORMATIONAL.serialize(),\n            HistoryEventSubType.NONE.serialize(),\n            str(-FVal(event_row[1])),\n            str(-FVal(event_row[2])),\n            \'Automatic virtual conversion of staked ETH rewards to ETH\',\n            event_row[0],\n        ) for event_row in write_cursor]\n        if len(update_tuples) != 0:\n            write_cursor.executemany(\n                \'UPDATE history_events SET type=?, subtype=?, amount=?, usd_value=?, notes=? WHERE identifier=?\',  # noqa: E501\n                update_tuples,\n            )\n\n        log.debug(\'Fixing kraken trades\')\n        write_cursor.execute(\n            "SELECT identifier, amount, usd_value FROM history_events WHERE location=\'B\' AND "\n            "type=\'trade\' AND subtype=\'none\'",\n        )\n\n        trade_db_type = HistoryEventType.TRADE.serialize()\n        update_tuples = []\n        for event_row in write_cursor:\n            asset_amount = FVal(event_row[1])\n            usd_value = FVal(event_row[2])\n            if asset_amount < ZERO:\n                event_subtype = HistoryEventSubType.SPEND\n                asset_amount = -asset_amount\n                usd_value = -usd_value\n            else:\n                event_subtype = HistoryEventSubType.RECEIVE\n\n            update_tuples.append(\n                (\n                    trade_db_type,\n                    event_subtype.serialize(),\n                    str(asset_amount),\n                    str(usd_value),\n                    event_row[0],\n                ),\n            )\n\n        if len(update_tuples) != 0:\n            write_cursor.executemany(\n                \'UPDATE history_events SET type=?, subtype=?, amount=?, usd_value=? WHERE identifier=?\',  # noqa: E501\n                update_tuples,\n            )\n\n        log.debug(\'Fixing kraken withdrawals\')  # deposits are all positive amount already\n        write_cursor.execute(\n            "SELECT identifier, amount, usd_value FROM history_events WHERE location=\'B\' AND "\n            "type=\'withdrawal\'",\n        )\n        update_tuples = [(\n            str(-FVal(event_row[1])),\n            str(-FVal(event_row[2])),\n            event_row[0],\n        ) for event_row in write_cursor]\n        if len(update_tuples) != 0:\n            write_cursor.executemany(\n                \'UPDATE history_events SET amount=?, usd_value=? WHERE identifier=?\',\n                update_tuples,\n            )\n\n        log.debug(\'Fixing kraken instant swaps\')\n        update_tuples = []\n        grouped_events: dict[str, list[Any]] = defaultdict(list)\n        write_cursor.execute(\n            "SELECT identifier, event_identifier, type, amount, usd_value FROM history_events "\n            "WHERE location=\'B\' AND type IN (\'spend\', \'receive\') AND subtype=\'none\'",\n        )\n        for row in write_cursor:\n            grouped_events[row[1]].append(row)\n\n        for events in grouped_events.values():\n            if len(events) != 2:\n                # we are looking for instant swaps that are always pairs of spend/receive\n                # with the same event identifier\n                continue\n\n            for event in events:\n                if event[2] == \'spend\':\n                    event_subtype = HistoryEventSubType.SPEND\n                    asset_amount = FVal(event[3])\n                    asset_amount_str = str(-asset_amount) if asset_amount != ZERO else str(asset_amount)  # noqa: E501\n                    usd_value_str = str(-FVal(event[4]))\n                else:\n                    event_subtype = HistoryEventSubType.RECEIVE\n                    asset_amount_str = event[3]\n                    usd_value_str = event[4]\n\n                update_tuples.append(\n                    (\n                        trade_db_type,\n                        event_subtype.serialize(),\n                        asset_amount_str,\n                        usd_value_str,\n                        event[0],\n                    ),\n                )\n        if len(update_tuples) != 0:\n            write_cursor.executemany(\n                \'UPDATE history_events SET type=?, subtype=?, amount=?, usd_value=? WHERE identifier=?\',  # noqa: E501\n                update_tuples,\n            )\n\n    @progress_step(description=\'Trimming daily stats.\')\n    def _trim_daily_stats(write_cursor: \'DBCursor\') -> None:\n        """Decreases the amount of data in the daily stats table"""\n        update_table_schema(\n            write_cursor=write_cursor,\n            table_name=\'eth2_daily_staking_details\',\n            schema="""validator_index INTEGER NOT NULL,\n            timestamp INTEGER NOT NULL,\n            pnl TEXT NOT NULL,\n            FOREIGN KEY(validator_index) REFERENCES eth2_validators(validator_index) ON UPDATE CASCADE ON DELETE CASCADE,\n            PRIMARY KEY (validator_index, timestamp)""",  # noqa: E501\n            insert_columns=\'validator_index, timestamp, pnl\',\n            insert_where=\'start_amount != 0 OR end_amount !=0 OR amount_deposited != 0\',\n        )\n\n    @progress_step(description=\'Removing FTX data.\')\n    def _remove_ftx_data(write_cursor: \'DBCursor\') -> None:\n        """Removes FTX-related settings from the DB"""\n        write_cursor.execute(\n            \'DELETE FROM user_credentials WHERE location IN (?, ?)\',\n            (Location.FTX.serialize_for_db(), Location.FTXUS.serialize_for_db()),\n        )\n        write_cursor.execute(\n            \'DELETE FROM user_credentials_mappings WHERE credential_location IN (?, ?)\',\n            (Location.FTX.serialize_for_db(), Location.FTXUS.serialize_for_db()),\n        )\n        write_cursor.execute(\n            \'DELETE FROM used_query_ranges WHERE name LIKE ? ESCAPE ?;')
    op.execute('INSERT INTO history_events_copy VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)\', new_entries)  # noqa: E501\n        for entry in extra_evm_info_entries:\n            write_cursor.execute(\n                \'INSERT INTO evm_events_info VALUES(?, ?, ?, ?, ?, ?)\',\n                (entry[0], entry[1], entry[2], None, None, entry[3]),\n            )\n\n        write_cursor.switch_foreign_keys(\'OFF\')  # need FK off or history_events_mappings get deleted  # noqa: E501\n        write_cursor.execute(\'DROP TABLE history_events\')\n        write_cursor.execute(\'ALTER TABLE history_events_copy RENAME TO history_events\')\n        write_cursor.switch_foreign_keys(\'ON\')\n\n    @progress_step(description=\'Updating ens mappings schema.\')\n    def _update_ens_mappings_schema(write_cursor: \'DBCursor\') -> None:\n        update_table_schema(\n            write_cursor=write_cursor,\n            table_name=\'ens_mappings\',\n            schema="""address TEXT NOT NULL PRIMARY KEY,\n            ens_name TEXT UNIQUE,\n            last_update INTEGER NOT NULL,\n            last_avatar_update INTEGER NOT NULL DEFAULT 0""",\n            insert_columns=\'address, ens_name, last_update\',\n            insert_order=\'(address, ens_name, last_update)\',\n        )\n\n    @progress_step(description=\'Deleting old tables.\')\n    def _delete_old_tables(write_cursor: \'DBCursor\') -> None:\n        """Deletes old tables that are now unused along with related data\n        """\n        write_cursor.execute(\'DROP TABLE IF EXISTS eth2_deposits\')\n        write_cursor.execute(\n            \'DELETE FROM used_query_ranges WHERE name LIKE ?\',\n            (\'eth2_deposits%\',),\n        )\n\n    @progress_step(description=\'Fixing Kraken events.\')\n    def _fix_kraken_events(write_cursor: \'DBCursor\') -> None:\n        """\n        Fix kraken events with negative amounts related to:\n        - staking ETH after the merge\n        - trades not having subtypes and having negative amounts\n        - withdrawals use positive amounts\n        - instant swaps\n        Needs to be executed after _update_history_events_schema\n        """\n        write_cursor.execute(\n            "SELECT identifier, amount, usd_value FROM history_events WHERE location=\'B\' AND "\n            "asset=? AND type=\'staking\' AND subtype=\'reward\' AND CAST(amount AS REAL) < 0",\n            (A_ETH2.identifier,),\n        )\n        update_tuples: list[Any] = [(\n            HistoryEventType.INFORMATIONAL.serialize(),\n            HistoryEventSubType.NONE.serialize(),\n            str(-FVal(event_row[1])),\n            str(-FVal(event_row[2])),\n            \'Automatic virtual conversion of staked ETH rewards to ETH\',\n            event_row[0],\n        ) for event_row in write_cursor]\n        if len(update_tuples) != 0:\n            write_cursor.executemany(\n                \'UPDATE history_events SET type=?, subtype=?, amount=?, usd_value=?, notes=? WHERE identifier=?\',  # noqa: E501\n                update_tuples,\n            )\n\n        log.debug(\'Fixing kraken trades\')\n        write_cursor.execute(\n            "SELECT identifier, amount, usd_value FROM history_events WHERE location=\'B\' AND "\n            "type=\'trade\' AND subtype=\'none\'",\n        )\n\n        trade_db_type = HistoryEventType.TRADE.serialize()\n        update_tuples = []\n        for event_row in write_cursor:\n            asset_amount = FVal(event_row[1])\n            usd_value = FVal(event_row[2])\n            if asset_amount < ZERO:\n                event_subtype = HistoryEventSubType.SPEND\n                asset_amount = -asset_amount\n                usd_value = -usd_value\n            else:\n                event_subtype = HistoryEventSubType.RECEIVE\n\n            update_tuples.append(\n                (\n                    trade_db_type,\n                    event_subtype.serialize(),\n                    str(asset_amount),\n                    str(usd_value),\n                    event_row[0],\n                ),\n            )\n\n        if len(update_tuples) != 0:\n            write_cursor.executemany(\n                \'UPDATE history_events SET type=?, subtype=?, amount=?, usd_value=? WHERE identifier=?\',  # noqa: E501\n                update_tuples,\n            )\n\n        log.debug(\'Fixing kraken withdrawals\')  # deposits are all positive amount already\n        write_cursor.execute(\n            "SELECT identifier, amount, usd_value FROM history_events WHERE location=\'B\' AND "\n            "type=\'withdrawal\'",\n        )\n        update_tuples = [(\n            str(-FVal(event_row[1])),\n            str(-FVal(event_row[2])),\n            event_row[0],\n        ) for event_row in write_cursor]\n        if len(update_tuples) != 0:\n            write_cursor.executemany(\n                \'UPDATE history_events SET amount=?, usd_value=? WHERE identifier=?\',\n                update_tuples,\n            )\n\n        log.debug(\'Fixing kraken instant swaps\')\n        update_tuples = []\n        grouped_events: dict[str, list[Any]] = defaultdict(list)\n        write_cursor.execute(\n            "SELECT identifier, event_identifier, type, amount, usd_value FROM history_events "\n            "WHERE location=\'B\' AND type IN (\'spend\', \'receive\') AND subtype=\'none\'",\n        )\n        for row in write_cursor:\n            grouped_events[row[1]].append(row)\n\n        for events in grouped_events.values():\n            if len(events) != 2:\n                # we are looking for instant swaps that are always pairs of spend/receive\n                # with the same event identifier\n                continue\n\n            for event in events:\n                if event[2] == \'spend\':\n                    event_subtype = HistoryEventSubType.SPEND\n                    asset_amount = FVal(event[3])\n                    asset_amount_str = str(-asset_amount) if asset_amount != ZERO else str(asset_amount)  # noqa: E501\n                    usd_value_str = str(-FVal(event[4]))\n                else:\n                    event_subtype = HistoryEventSubType.RECEIVE\n                    asset_amount_str = event[3]\n                    usd_value_str = event[4]\n\n                update_tuples.append(\n                    (\n                        trade_db_type,\n                        event_subtype.serialize(),\n                        asset_amount_str,\n                        usd_value_str,\n                        event[0],\n                    ),\n                )\n        if len(update_tuples) != 0:\n            write_cursor.executemany(\n                \'UPDATE history_events SET type=?, subtype=?, amount=?, usd_value=? WHERE identifier=?\',  # noqa: E501\n                update_tuples,\n            )\n\n    @progress_step(description=\'Trimming daily stats.\')\n    def _trim_daily_stats(write_cursor: \'DBCursor\') -> None:\n        """Decreases the amount of data in the daily stats table"""\n        update_table_schema(\n            write_cursor=write_cursor,\n            table_name=\'eth2_daily_staking_details\',\n            schema="""validator_index INTEGER NOT NULL,\n            timestamp INTEGER NOT NULL,\n            pnl TEXT NOT NULL,\n            FOREIGN KEY(validator_index) REFERENCES eth2_validators(validator_index) ON UPDATE CASCADE ON DELETE CASCADE,\n            PRIMARY KEY (validator_index, timestamp)""",  # noqa: E501\n            insert_columns=\'validator_index, timestamp, pnl\',\n            insert_where=\'start_amount != 0 OR end_amount !=0 OR amount_deposited != 0\',\n        )\n\n    @progress_step(description=\'Removing FTX data.\')\n    def _remove_ftx_data(write_cursor: \'DBCursor\') -> None:\n        """Removes FTX-related settings from the DB"""\n        write_cursor.execute(\n            \'DELETE FROM user_credentials WHERE location IN (?, ?)\',\n            (Location.FTX.serialize_for_db(), Location.FTXUS.serialize_for_db()),\n        )\n        write_cursor.execute(\n            \'DELETE FROM user_credentials_mappings WHERE credential_location IN (?, ?)\',\n            (Location.FTX.serialize_for_db(), Location.FTXUS.serialize_for_db()),\n        )\n        write_cursor.execute(\n            \'DELETE FROM used_query_ranges WHERE name LIKE ? ESCAPE ?;')
    op.create_table('evm_events_info', ...)
    op.execute('UPDATE CASCADE ON DELETE CASCADE\n        );')
    op.execute('UPDATE CASCADE,\n        UNIQUE(event_identifier, sequence_index)\n        );')
    op.execute('UPDATE history_events SET type=?, subtype=?, amount=?, usd_value=?, notes=? WHERE identifier=?\',  # noqa: E501\n                update_tuples,\n            )\n\n        log.debug(\'Fixing kraken trades\')\n        write_cursor.execute(\n            "SELECT identifier, amount, usd_value FROM history_events WHERE location=\'B\' AND "\n            "type=\'trade\' AND subtype=\'none\'",\n        )\n\n        trade_db_type = HistoryEventType.TRADE.serialize()\n        update_tuples = []\n        for event_row in write_cursor:\n            asset_amount = FVal(event_row[1])\n            usd_value = FVal(event_row[2])\n            if asset_amount < ZERO:\n                event_subtype = HistoryEventSubType.SPEND\n                asset_amount = -asset_amount\n                usd_value = -usd_value\n            else:\n                event_subtype = HistoryEventSubType.RECEIVE\n\n            update_tuples.append(\n                (\n                    trade_db_type,\n                    event_subtype.serialize(),\n                    str(asset_amount),\n                    str(usd_value),\n                    event_row[0],\n                ),\n            )\n\n        if len(update_tuples) != 0:\n            write_cursor.executemany(\n                \'UPDATE history_events SET type=?, subtype=?, amount=?, usd_value=? WHERE identifier=?\',  # noqa: E501\n                update_tuples,\n            )\n\n        log.debug(\'Fixing kraken withdrawals\')  # deposits are all positive amount already\n        write_cursor.execute(\n            "SELECT identifier, amount, usd_value FROM history_events WHERE location=\'B\' AND "\n            "type=\'withdrawal\'",\n        )\n        update_tuples = [(\n            str(-FVal(event_row[1])),\n            str(-FVal(event_row[2])),\n            event_row[0],\n        ) for event_row in write_cursor]\n        if len(update_tuples) != 0:\n            write_cursor.executemany(\n                \'UPDATE history_events SET amount=?, usd_value=? WHERE identifier=?\',\n                update_tuples,\n            )\n\n        log.debug(\'Fixing kraken instant swaps\')\n        update_tuples = []\n        grouped_events: dict[str, list[Any]] = defaultdict(list)\n        write_cursor.execute(\n            "SELECT identifier, event_identifier, type, amount, usd_value FROM history_events "\n            "WHERE location=\'B\' AND type IN (\'spend\', \'receive\') AND subtype=\'none\'",\n        )\n        for row in write_cursor:\n            grouped_events[row[1]].append(row)\n\n        for events in grouped_events.values():\n            if len(events) != 2:\n                # we are looking for instant swaps that are always pairs of spend/receive\n                # with the same event identifier\n                continue\n\n            for event in events:\n                if event[2] == \'spend\':\n                    event_subtype = HistoryEventSubType.SPEND\n                    asset_amount = FVal(event[3])\n                    asset_amount_str = str(-asset_amount) if asset_amount != ZERO else str(asset_amount)  # noqa: E501\n                    usd_value_str = str(-FVal(event[4]))\n                else:\n                    event_subtype = HistoryEventSubType.RECEIVE\n                    asset_amount_str = event[3]\n                    usd_value_str = event[4]\n\n                update_tuples.append(\n                    (\n                        trade_db_type,\n                        event_subtype.serialize(),\n                        asset_amount_str,\n                        usd_value_str,\n                        event[0],\n                    ),\n                )\n        if len(update_tuples) != 0:\n            write_cursor.executemany(\n                \'UPDATE history_events SET type=?, subtype=?, amount=?, usd_value=? WHERE identifier=?\',  # noqa: E501\n                update_tuples,\n            )\n\n    @progress_step(description=\'Trimming daily stats.\')\n    def _trim_daily_stats(write_cursor: \'DBCursor\') -> None:\n        """Decreases the amount of data in the daily stats table"""\n        update_table_schema(\n            write_cursor=write_cursor,\n            table_name=\'eth2_daily_staking_details\',\n            schema="""validator_index INTEGER NOT NULL,\n            timestamp INTEGER NOT NULL,\n            pnl TEXT NOT NULL,\n            FOREIGN KEY(validator_index) REFERENCES eth2_validators(validator_index) ON UPDATE CASCADE ON DELETE CASCADE,\n            PRIMARY KEY (validator_index, timestamp)""",  # noqa: E501\n            insert_columns=\'validator_index, timestamp, pnl\',\n            insert_where=\'start_amount != 0 OR end_amount !=0 OR amount_deposited != 0\',\n        )\n\n    @progress_step(description=\'Removing FTX data.\')\n    def _remove_ftx_data(write_cursor: \'DBCursor\') -> None:\n        """Removes FTX-related settings from the DB"""\n        write_cursor.execute(\n            \'DELETE FROM user_credentials WHERE location IN (?, ?)\',\n            (Location.FTX.serialize_for_db(), Location.FTXUS.serialize_for_db()),\n        )\n        write_cursor.execute(\n            \'DELETE FROM user_credentials_mappings WHERE credential_location IN (?, ?)\',\n            (Location.FTX.serialize_for_db(), Location.FTXUS.serialize_for_db()),\n        )\n        write_cursor.execute(\n            \'DELETE FROM used_query_ranges WHERE name LIKE ? ESCAPE ?;')
    op.execute('DELETE FROM history_events WHERE event_identifier=?\'\n    if length != 0:\n        querystr += f\' AND identifier NOT IN ({", ".join(["?"] * length)})\'\n        bindings = [(x, *customized_event_ids) for x in tx_hashes]\n    else:\n        bindings = [(x,) for x in tx_hashes]\n    write_cursor.executemany(querystr, bindings)\n    write_cursor.executemany(\n        \'DELETE from evm_tx_mappings WHERE tx_hash=? AND value=?\',\n        [(tx_hash, 0) for tx_hash in tx_hashes],  # 0 -> decoded state\n    )\n\n\n@enter_exit_debug_log(name=\'UserDB v36->v37 upgrade\')\ndef upgrade_v36_to_v37(db: \'DBHandler\', progress_handler: \'DBUpgradeProgressHandler\') -> None:\n    """Upgrades the DB from v36 to v37. This was in v1.28.0 release.\n\n        - Replace null history event subtype\n    """\n    @progress_step(description=\'Moving event locations.\')\n    def _move_event_locations(write_cursor: \'DBCursor\') -> None:\n        """\n        Create location ethereum and optimism and move the blockchain events to those locations\n\n        This used to be DB data migration 9 which was added only for when upgrading from 1.27.0 to\n        1.27.1. So it only exists in 1.27.1 and has from now on (1.28.0) moved to this DB upgrade\n\n        This should always be at the start of this DB upgrade as it relies on the old schema.\n        """\n        write_cursor.execute("INSERT OR IGNORE INTO location(location, seq) VALUES (\'f\', 38);')
    op.create_table('evm_events_info', ...)
    op.execute('DELETE FROM used_query_ranges WHERE name LIKE ?\',\n            (\'eth2_deposits%\',),\n        )\n\n    @progress_step(description=\'Fixing Kraken events.\')\n    def _fix_kraken_events(write_cursor: \'DBCursor\') -> None:\n        """\n        Fix kraken events with negative amounts related to:\n        - staking ETH after the merge\n        - trades not having subtypes and having negative amounts\n        - withdrawals use positive amounts\n        - instant swaps\n        Needs to be executed after _update_history_events_schema\n        """\n        write_cursor.execute(\n            "SELECT identifier, amount, usd_value FROM history_events WHERE location=\'B\' AND "\n            "asset=? AND type=\'staking\' AND subtype=\'reward\' AND CAST(amount AS REAL) < 0",\n            (A_ETH2.identifier,),\n        )\n        update_tuples: list[Any] = [(\n            HistoryEventType.INFORMATIONAL.serialize(),\n            HistoryEventSubType.NONE.serialize(),\n            str(-FVal(event_row[1])),\n            str(-FVal(event_row[2])),\n            \'Automatic virtual conversion of staked ETH rewards to ETH\',\n            event_row[0],\n        ) for event_row in write_cursor]\n        if len(update_tuples) != 0:\n            write_cursor.executemany(\n                \'UPDATE history_events SET type=?, subtype=?, amount=?, usd_value=?, notes=? WHERE identifier=?\',  # noqa: E501\n                update_tuples,\n            )\n\n        log.debug(\'Fixing kraken trades\')\n        write_cursor.execute(\n            "SELECT identifier, amount, usd_value FROM history_events WHERE location=\'B\' AND "\n            "type=\'trade\' AND subtype=\'none\'",\n        )\n\n        trade_db_type = HistoryEventType.TRADE.serialize()\n        update_tuples = []\n        for event_row in write_cursor:\n            asset_amount = FVal(event_row[1])\n            usd_value = FVal(event_row[2])\n            if asset_amount < ZERO:\n                event_subtype = HistoryEventSubType.SPEND\n                asset_amount = -asset_amount\n                usd_value = -usd_value\n            else:\n                event_subtype = HistoryEventSubType.RECEIVE\n\n            update_tuples.append(\n                (\n                    trade_db_type,\n                    event_subtype.serialize(),\n                    str(asset_amount),\n                    str(usd_value),\n                    event_row[0],\n                ),\n            )\n\n        if len(update_tuples) != 0:\n            write_cursor.executemany(\n                \'UPDATE history_events SET type=?, subtype=?, amount=?, usd_value=? WHERE identifier=?\',  # noqa: E501\n                update_tuples,\n            )\n\n        log.debug(\'Fixing kraken withdrawals\')  # deposits are all positive amount already\n        write_cursor.execute(\n            "SELECT identifier, amount, usd_value FROM history_events WHERE location=\'B\' AND "\n            "type=\'withdrawal\'",\n        )\n        update_tuples = [(\n            str(-FVal(event_row[1])),\n            str(-FVal(event_row[2])),\n            event_row[0],\n        ) for event_row in write_cursor]\n        if len(update_tuples) != 0:\n            write_cursor.executemany(\n                \'UPDATE history_events SET amount=?, usd_value=? WHERE identifier=?\',\n                update_tuples,\n            )\n\n        log.debug(\'Fixing kraken instant swaps\')\n        update_tuples = []\n        grouped_events: dict[str, list[Any]] = defaultdict(list)\n        write_cursor.execute(\n            "SELECT identifier, event_identifier, type, amount, usd_value FROM history_events "\n            "WHERE location=\'B\' AND type IN (\'spend\', \'receive\') AND subtype=\'none\'",\n        )\n        for row in write_cursor:\n            grouped_events[row[1]].append(row)\n\n        for events in grouped_events.values():\n            if len(events) != 2:\n                # we are looking for instant swaps that are always pairs of spend/receive\n                # with the same event identifier\n                continue\n\n            for event in events:\n                if event[2] == \'spend\':\n                    event_subtype = HistoryEventSubType.SPEND\n                    asset_amount = FVal(event[3])\n                    asset_amount_str = str(-asset_amount) if asset_amount != ZERO else str(asset_amount)  # noqa: E501\n                    usd_value_str = str(-FVal(event[4]))\n                else:\n                    event_subtype = HistoryEventSubType.RECEIVE\n                    asset_amount_str = event[3]\n                    usd_value_str = event[4]\n\n                update_tuples.append(\n                    (\n                        trade_db_type,\n                        event_subtype.serialize(),\n                        asset_amount_str,\n                        usd_value_str,\n                        event[0],\n                    ),\n                )\n        if len(update_tuples) != 0:\n            write_cursor.executemany(\n                \'UPDATE history_events SET type=?, subtype=?, amount=?, usd_value=? WHERE identifier=?\',  # noqa: E501\n                update_tuples,\n            )\n\n    @progress_step(description=\'Trimming daily stats.\')\n    def _trim_daily_stats(write_cursor: \'DBCursor\') -> None:\n        """Decreases the amount of data in the daily stats table"""\n        update_table_schema(\n            write_cursor=write_cursor,\n            table_name=\'eth2_daily_staking_details\',\n            schema="""validator_index INTEGER NOT NULL,\n            timestamp INTEGER NOT NULL,\n            pnl TEXT NOT NULL,\n            FOREIGN KEY(validator_index) REFERENCES eth2_validators(validator_index) ON UPDATE CASCADE ON DELETE CASCADE,\n            PRIMARY KEY (validator_index, timestamp)""",  # noqa: E501\n            insert_columns=\'validator_index, timestamp, pnl\',\n            insert_where=\'start_amount != 0 OR end_amount !=0 OR amount_deposited != 0\',\n        )\n\n    @progress_step(description=\'Removing FTX data.\')\n    def _remove_ftx_data(write_cursor: \'DBCursor\') -> None:\n        """Removes FTX-related settings from the DB"""\n        write_cursor.execute(\n            \'DELETE FROM user_credentials WHERE location IN (?, ?)\',\n            (Location.FTX.serialize_for_db(), Location.FTXUS.serialize_for_db()),\n        )\n        write_cursor.execute(\n            \'DELETE FROM user_credentials_mappings WHERE credential_location IN (?, ?)\',\n            (Location.FTX.serialize_for_db(), Location.FTXUS.serialize_for_db()),\n        )\n        write_cursor.execute(\n            \'DELETE FROM used_query_ranges WHERE name LIKE ? ESCAPE ?;')
    op.execute('DELETE FROM used_query_ranges WHERE name LIKE ? ESCAPE ?;')



def downgrade() -> None:
    """Downgrade from v37 to v36"""
    # Note: Downgrades were not supported in the original system
    # These would need to be implemented based on the upgrade operations
    raise NotImplementedError("Downgrade not implemented for this migration")
